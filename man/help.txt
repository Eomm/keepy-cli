
Install via npm:
  npm i keepy-cli -g

Usage: keepy <command>

where <command> is one of:
  init, add, delete, restore, help

keepy <command> -h  quick help on <command>

Usage via npx:
  npx keepy-cli init